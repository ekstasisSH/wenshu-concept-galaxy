# 文枢·概念宇宙 M4 视觉诊断与精修意见（对标诗云）

> 日期：2026-08-17 ｜ 对象：`galaxy/dist/concept_galaxy.html`（M4 审美精修后版本）
> 参照：`07_参考文件/shiyun-main`（源码 + docs/assets 截图 + FRONTEND_GUIDE.md）
> 性质：仅诊断与建议，不含代码修改

---

## 一、核心结论

**当前产物"粗糙感"的根源不是参数，而是渲染技术路线差异**：诗云的所有星体（诗人星、星系背景、虚空标记）全部是**自定义 shader 点精灵**（高斯衰减 + 加色混合 + 微闪烁），没有任何一个实体球体；我们的概念/篇目节点是 **MeshPhongMaterial 镜面球体**——带高光、阴影边的"玻璃弹珠"质感，与"深空星云"主题天然冲突。M4 精修加的光晕贴片只是在弹珠外面罩了一层雾，弹珠本体仍在。

次要根源：配色色相过多（12+ 饱和色），UI 强调色用冷蓝而非云式暖金，面板/图例组件语言不统一。

---

## 二、参考分析：诗云的设计体系

来源：shiyun-main/src/styles.css、src/three/PoetStars.tsx、galaxyParams.ts、docs/assets/*.jpg、docs/FRONTEND_GUIDE.md

| 维度 | 诗云做法 | 出处 |
|---|---|---|
| 星体渲染 | 点精灵 shader：`smoothstep(0.5,0.03,d)` 高斯衰减、加色混合、亮度×2.3、`sin(uTime)` 微闪烁、尺寸随距离 `clamp(aSize·k/-z, 1.2, 70)` | PoetStars.tsx:75-100 |
| 层级手段 | 靠**尺寸+明度**区分层级（名诗人 2.4× 大、向白 lerp 0.22 镀金、×1.8 提亮），不靠色相 | PoetStars.tsx:24-27,56 |
| 星系背景 | ~16.6 万粒子：尘埃+臂星+核球三群体，暖核→蓝臂渐变色，指数盘+值噪声结团 | FRONTEND_GUIDE.md §6 |
| 弧线 | 三次贝塞尔向心收束（BUNDLE=0.3），自定义 shader 沿弧脉冲流动，端点渐隐 | FRONTEND_GUIDE.md §6 GiftLines |
| 配色 | 墨底 `#04050a`，**唯一强调色金 `#ffd27a`**，主文字 `#e8ecf4`，次要 `#8b93a7`；星云本体 pastel 粉/绿/金 | styles.css:1-6 |
| 字体 | UI 无衬线（PingFang/雅黑），**内容用楷/宋 serif 栈**，字距 0.06–0.25em，数字 tabular-nums | styles.css:7-10 |
| 面板 | 右侧垂直居中 340px，blur(16px)，金色 0.18 透明度边框，顶部径向金色渐变+金色发线，圆角 16px，rise 入场动画，金色细滚动条 | styles.css:228-256 |
| 图例 | 左侧垂直居中窄条（132px），9px 圆点带 `box-shadow` 发光，关闭态=透明 0.32+删除线 | styles.css:142-225 |
| 交互 | hover 只浮现文字标签（0.5→1 不透明），十字光标，H 键隐藏全部 UI，画质高/低切换，reduced-motion 降级 | FRONTEND_GUIDE.md §5, styles.css:1191 |
| 布局 | HUD 顶栏左标题右统计，搜索贴左上图标下，面板右中，图例左中，提示底栏通栏——四角+边中，互不遮挡 | styles.css 各 fixed 定位 |

---

## 三、全面诊断（按维度）

### 1. 布局结构
- 五个角落全部被占用（顶中 HUD、左上图例、左下搜索、右下按钮、底中统计），视觉重心被切碎；图例三段式（节点类型/关系/卷别）纵向堆叠过长。
- 面板固定右上，与图例/搜索的"贴边"逻辑不一致（诗云面板统一右侧居中）。

### 2. 配色方案
- 实体球颜色饱和度高且色相多：概念蓝、理论紫、人物绿、篇目四卷四色、关系三色、金——同屏 12+ 高饱和色，呈"糖果盘"效应。
- UI 强调色是冷蓝 `#7fb0ff`，与毛泽东金色 `#ffd24d` 并存两个强调色，互相抢戏。
- 背景螺旋星点单一蓝灰色 `#7a8fc0/#556a9a`，无暖核→冷臂的色彩纵深。

### 3. 字体排版
- 全文 Microsoft YaHei 单一字重，概念名/引文这类内容文本未用 serif（诗云用楷宋栈，是"人文感"的关键）。
- 字距单位混用 px（1px/2px/2.5px），未按字号比例用 em。
- 统计数字（1275 概念…）未用 tabular-nums，跳动时位数晃动。

### 4. 间距留白
- 图例行高 line-height:2 与 padding 叠加后偏松，而面板内 `.rel` 卡片 margin 5px 又偏挤，疏密无节奏标准（诗云以 4/6/8/10/12/14/16 阶梯统一）。
- HUD pill 内边距 4px 偏紧，文字 12.5px 配 2.5px 字距在小字号下显糊。

### 5. 视觉层级
- 层级靠"颜色种类"而非"明暗/大小梯度"——叶子节点降噪后仍是彩色小球，远看一片彩点，缺乏诗云"明星锚点 + 弥漫星尘"的二元结构。
- 篇目星带 ⭐ emoji 图例说明，但场景内篇目与其他节点只有颜色差别，无形态区分。
- 流动粒子亮青/绿色过亮，在加色混合下成为画面里最跳的元素，喧宾夺主。

### 6. 组件样式
- 节点=镜面球体（高光+菲涅尔暗边），是"粗糙感"首因；halo 贴片与球芯质感断裂。
- 图例圆点 10px 纯色无发光；关系类型用 18px 色条，与圆点语言不统一。
- 面板：直角信息密度高、无入场动画、无顶部 accent 线、滚动条为系统默认粗条。
- 按钮/输入框边框 rgba(120,160,255,.35~.5) 偏亮，呈现"蓝框堆积"。

### 7. 交互反馈
- hover 球体瞬间 ×1.3 放大，跳变生硬；诗云 hover 仅淡入文字标签，星体本身不动。
- tooltip 边框亮蓝 0.5 透明度，过重；无 hover 时相关边高亮的渐进过渡（opacity 直切）。
- 缺少：加载屏、首次引导、UI 一键隐藏（截图模式）、画质降级开关、键盘快捷键说明。
- 触摸屏只处理了 pointer 坐标，canvas 未设 `touch-action:none`，移动端手势会被浏览器拦截。

### 8. 响应式表现
- 无任何 media query：图例固定宽度、面板固定 340px，小屏直接遮挡画布一半以上。
- 诗云模式是 `width:min(384px, calc(100vw - 36px))`，可借鉴。
- 无 `prefers-reduced-motion` 支持（开场缓入/自动旋转不可降级）。

---

## 四、修改意见清单（按优先级）

> 说明：本轮不动代码；标注的"依据"为诗云文件位置或通用设计原则。

### 高优先级（决定"精致感"的 80%）

| # | 问题 | 参考依据 | 改进方向 |
|---|---|---|---|
| H1 | 节点为镜面球体，弹珠质感与星云主题冲突 | 诗云 PoetStars.tsx:75-100 全点精灵 | 概念/被引节点改为 ShaderMaterial 点精灵：高斯衰减 `smoothstep(0.5,0.05,d)` + 加色混合 + 亮度×2 左右 + 距离尺寸衰减；毛泽东与篇目星保留实体感（或更大点精灵+现有 halo），形成"恒星 vs 星尘"二元结构 |
| H2 | 色相过多（12+），糖果盘 | 诗云仅靠尺寸/明度分层（PoetStars.tsx:24-27,56） | 收敛色相：概念统一为暖白~蓝白渐变（按卷或按度），理论/人物仅微调明度不靠新色相；篇目星保留卷色但降饱和（×0.7）；强调色只留金 |
| H3 | UI 强调色冷蓝与金色双 accent 打架 | 诗云全局唯一金 accent（styles.css:3） | UI accent 全部改金系（边框 rgba(255,210,122,.2~.5)、hover 金底 8~16% 透明）；冷蓝仅保留给"被引文献"语义 |
| H4 | 组件语言不统一（圆点/色条/亮边框） | 诗云图例 styles.css:219-225 | 图例统一 9px 发光圆点（box-shadow:0 0 6px currentColor）；关系类型也改圆点或短线+发光；边框统一 0.08~0.14 低透明；关闭态改"透明 0.32+删除线"替代灰显 |
| H5 | 流动粒子过亮喧宾夺主 | 截图目测 + 诗云"脉冲沿弧、端点渐隐"（FRONTEND_GUIDE §6） | 粒子亮度/尺寸降 30~40%，颜色向所属关系色深处压（明度↓饱和度↑），或加首尾 fade-in/out 生命周期 |

### 中优先级（质感与专业度）

| # | 问题 | 参考依据 | 改进方向 |
|---|---|---|---|
| M1 | 内容文本无 serif，缺人文感 | 诗云 --serif 楷宋栈（styles.css:7-10） | 面板标题、概念释义、出处引文改用 `"Kaiti SC","STKaiti",KaiTi,"Noto Serif SC",serif`；正文释义字号 13→14px、行高 1.8 |
| M2 | 面板无动效与 accent 细节 | 诗云 poem-panel（styles.css:228-256） | 面板改右侧垂直居中；加顶部金色发线（::before 渐变线）+ radial 金渐变底 + rise 0.3s 入场；滚动条改 8px 金色细条 |
| M3 | hover 球体 ×1.3 跳变生硬 | 诗云 poet-label 淡入（styles.css:43-56） | hover 改为：球体不动，节点名标签以 0.5→1 透明度浮出（sprite/HTML label）；聚焦边 opacity 加 0.15s transition |
| M4 | 布局切碎、图例过长 | 诗云边中布局 | 图例改左侧垂直居中窄条（~132px），三段合并为一段分组列表；统计挪入 HUD 右侧；搜索框上移贴 HUD 左下，底部仅留操作提示通栏 |
| M5 | 背景星点单色无纵深 | 诗云暖核→蓝臂（FRONTEND_GUIDE §6） | 螺旋星点按半径插色：内暖（#d8c8a8）→外冷蓝（#5a6c9a），尺寸/透明度随半径衰减；尘埃壳加少量暖色点 |
| M6 | 数字无 tabular-nums，字距用 px | 通用排版规范 + 诗云 styles.css | 统计/计数 `font-variant-numeric:tabular-nums`；字距改 em 制（小字 0.05em、标题 0.12~0.18em） |

### 低优先级（完整度与健壮性）

| # | 问题 | 参考依据 | 改进方向 |
|---|---|---|---|
| L1 | 无加载屏/引导 | 诗云 loading-screen + onb（styles.css:1502+,1690+） | 加黑底金字加载屏（标题 0.4em 字距）；首次访问 3 步引导卡 |
| L2 | 无 UI 隐藏/画质开关 | 诗云 H 键 + 画质高低（FRONTEND_GUIDE §4-5） | H 键隐藏全部覆盖层（演示截图用）；弱 GPU 时降粒子/关 bloom |
| L3 | 无响应式与触屏适配 | 诗云 min() 宽度模式 + touch-action:none | 面板/图例 `width:min(340px, calc(100vw-32px))`；canvas `touch-action:none`；小屏图例折叠为按钮 |
| L4 | 无 reduced-motion 降级 | 诗云 styles.css:1191 | `prefers-reduced-motion` 时跳过开场缓入、停自动旋转 |
| L5 | tooltip 边框过重、z-index 体系随意（10/20 混用） | 通用规范 | tooltip 边框降至 0.2 透明或无框+阴影；z-index 分层表文档化（场景 0 / HUD 10 / 面板 25 / 提示 30） |

---

## 五、实施建议顺序

H1（点精灵化）→ H2（配色收敛）→ H3（金色 accent）→ H4（图例统一）→ H5（粒子降亮）→ M 系列 → L 系列。

H1 是唯一涉及渲染管线改动的项（需新 shader，替代 MeshPhongMaterial），建议单独一轮"改→截图→评"；H2-H5 均为 CONFIG/样式级调整，可延续 M4 的逐项精修流程。

---

*诊断依据：shot_m4_s5_final.png（当前）、shiyun-main docs/assets 四张实拍图、双方源码。所有诗云引用均已标注文件与行号。*

# 文枢·概念宇宙 结构化改造方案（日心体系重构）

> 日期：2026-08-17 ｜ 依据：《评估报告_M4视觉诊断》《评估报告_形态与主题契合诊断》
> 总策略：不打散重建，"换骨级修改"——保留管线与交互骨架（约 70% 代码），重做布局数据层 + 渲染皮层
> 时间锚点：8/20 报名+BP 初稿 → 8/29 校赛。改造窗口 8/17–8/19，三天三段

---

## 一、改造目标（一句话）

让"一人一宇宙"在三维构图上成立：**毛泽东=恒星居绝对中心，137 篇目=按卷分四环轨道的行星，1275 概念=环绕主篇目的卫星星尘，从内向外=1926→1949 的革命时间轴。**

视觉叙事：中心一颗金色恒星 → 四圈彩色行星环（卷一浅蓝最内 → 卷四青绿最外）→ 每颗行星周围簇拥自己的概念星尘 → 跨篇关系弧如彗星尾划破轨道。

## 二、不可动红线（保持回归能力）

1. JSON schema 不变（`n/t/p/s/d/vol/anch/src`、`links` 字段原样）——只改 `p` 的算法
2. `_test2.mjs` 断言全部保留并扩展（见 5.3）
3. 构建/验证命令不变：`rm -f dist/concept_galaxy.html && node build.mjs && node _test2.mjs`
4. 交互行为不变：hover/点击/面板/搜索/飞行/筛选的对外表现一致
5. 动手前先快照：`git init && git add -A && git commit`（或整目录复制为 `galaxy_m4_backup/`）

## 三、段一：换骨（数据布局重构）—— ✅ 已完成（2026-08-17，commit cd3c9bd）

**只改 `gen_galaxy_data.py` 第 4-5 节（位置计算），其余数据链不动。实测结果：**

- 中心恒星 r=0.0（断言 <0.5 ✓）
- 四环半径均值 70.1 / 125.2 / 181.0 / 234.7（目标 70/125/180/235，±15 断言 ✓）
- 卫星距主篇目 p95=21.1（上限 28，断言 ≤36 ✓）
- 卷别归因：null 151 → 29（剩余为中心恒星+外部节点）
- 骨干边 706 → 429（两端度≥3），边噪明显下降
- 各卷篇目数：卷一 17 / 卷二 33 / 卷三 31 / 卷四 56
- 截图 `shot_s1_helio.png`：盘式结构成立，外环（卷四青绿）清晰可辨，金色中心可读

### 3.1 日心坐标算法

| 层 | 算法 | 参数（初值，看图调） |
|---|---|---|
| 恒星 | 毛泽东固定原点 `[0,0,0]` | — |
| 行星（篇目） | 按卷分环：环半径 r=卷序×55+15（卷一 70 / 卷二 125 / 卷三 180 / 卷四 235）；环内角位=篇目在卷内页码序×黄金角微扰，使时序相邻者角位相邻；环倾角各 ±6° 内随机，避免共面平板感 | `RING_GAP=55, RING_BASE=70, TILT=6°` |
| 卫星（概念） | `概念位置 = 主篇目位置 + 局部偏移`；局部偏移取原 PCA 语义坐标的簇内相对向量，归一化后缩放到 ≤28；无主篇目者（跨篇概念）按连接度加权的多篇目质心 + 偏移 | `SAT_R=28` |
| 外部节点 | 被引文献+ghost 合并为"外部星尘"，散布在最外环之外 260~300 的稀疏壳，缩小降透明 | `SHELL=260~300` |

### 3.2 顺带修复（数据层顺手项）

- 概念节点新增导出字段 `vol`（继承主篇目卷别，现在 151 个 null 需归因；null 者按连接篇目多数卷归化）——配色收敛（段二）依赖此字段
- 被引/ghost 合并标记 `t:'ext'`，图例减一项
- `bb` 骨干规则收紧：两端连接度 ≥3（706 条→约减半，治 S6 边噪）

### 3.3 段一验收

- [ ] 数值断言新增：毛泽东 `p=[0,0,0]`；各卷篇目环半径误差 ±15；概念距主篇目 ≤ SAT_R×1.2
- [ ] 截图目测：四环可辨、中心留白、金色恒星视觉主导
- [ ] 旧断言不回归（1275/137/1242 级别数量、毛泽东 107 连接）

## 四、段二：换皮（渲染与配色）

**只改 `app.js` 的 makeConceptMesh/材质 与 CONFIG、配色表。**

| # | 任务 | 做法 | 依据 |
|---|---|---|---|
| P1 | 概念节点点精灵化 | ShaderMaterial：高斯衰减 `smoothstep(0.5,0.05,d)`、加色混合、亮度×1.8、`gl_PointSize` 距离衰减 clamp(1.2,48)、`sin(uTime)` 微闪烁 | 诗云 PoetStars.tsx:75-100 |
| P2 | 拾取适配 | Points 拾取用 `raycaster.params.Points.threshold`；失效则保留不可见小球做 hit-proxy | 保交互红线 |
| P3 | 配色收敛 | 概念继承卷色×降饱和 0.55；篇目星保留卷色全饱和；理论/人物不再独立色相，用明度区分；UI accent 全改金系 `#ffd27a` | H2/H3 |
| P4 | 篇目星/毛泽东保留实体球+halo | 维持"恒星行星重、星尘轻"的二元材质结构 | 诊断 2.4 |
| P5 | 背景退化 | 旋臂粒子密度-50%、亮度-40%、暖核→冷臂径向渐变；不再横贯主体（倾角调整） | S3/S5 |
| P6 | 流动粒子降亮 | 亮度/尺寸 -35%，颜色明度压低 | H5 |

**段二验收**：截图对比——星尘呈"雾"而非"珠"；同屏色相 ≤5（四卷色+金）；零 console 错误；hover/点击/搜索回归人工过一遍。

## 五、段三：抛光（UI/排版/动效）—— ✅ 已实施（2026-08-17 前置完成）

> 展示层与日心布局解耦（不依赖位置数据），故提前实施。验证：`shot_ui_main.png`（全景）+ `shot_ui_panel.png`（面板），回归断言全过、零 console 错误。

### 5.0 现状评估结论（实施前诊断）

- 布局：居中药丸 HUD 与底中 stat 形成"双中心"；图例贴左上过长；面板贴右上与全局错位
- 色彩：冷蓝 `#7fb0ff` accent 与金色双 accent 冲突；关系 chip、边框、发光全蓝系
- 字体：全站无衬线单一字重，释义/出处等内容文本无人文感；数字无 tabular-nums
- 层级：面板内标题/标签/释义/依据四级无对比，"可溯源"卖点（页码+依据引文）埋在灰字里
- 交互：hover 球体瞬时 ×1.3 跳变生硬；无过渡动效；无焦点态

### 5.1 视觉层级优化（已落地）

| 手段 | 落地 |
|---|---|
| 信息架构 | 顶栏通栏化：左品牌（serif 金字 16px/.14em）+ 右统计（11px tabular-nums）；底部金色渐变发线 |
| 面板四级层级 | h3 serif 19px 金 → tag 金边胶囊（10.5px）→ 释义 serif 14px/1.85 → 依据引用条（serif+金左线 2px） |
| 卖点视觉化 | 出处页码金色高亮（tabular-nums）；"依据"统一改引用条，溯源链一眼可读 |
| 卡片分区 | 关系卡片 hover 泛金 8% 底；面板头 radial 金渐变 + 顶部金色发线 |

### 5.2 设计系统统一（已落地，CSS 令牌化）

- 令牌：`--ink:#05070f --gold:#ffd27a --gold-soft:#f6dca6 --fg:#e9edf5 --muted:#8b93a7 --line:rgba(255,210,122,.14) --panel-bg:rgba(10,13,24,.78)` + `--serif/--sans` 双字体栈
- 圆角体系：面板 16 / 图例 14 / 输入与按钮 10 / 卡片 9 / 胶囊 999
- 阴影/模糊：面板 blur(18px)+`0 16px 60px`；图例 blur(16px)+`0 12px 40px`；tooltip blur(10px) 无边框化
- 图标/记号：图例圆点 9px `box-shadow:0 0 6px currentColor` 发光；关闭态=透明 .32+删除线（文字包 `.lbl` span，flex 下删除线才可渲染）
- 主题契合："日心"意象由金色恒星（毛泽东）辐射到 UI——accent 全部取恒星金；关系三色/节点类型色属"语义色"保留，金色只作品牌 accent

### 5.3 交互与动效（已落地）

- hover 球体：瞬时 ×1.3 → lerp 平滑至 1.18×（`enlarged` 动画池，每帧 0.18 趋近，离场回落后自动出池）
- 面板入场 rise 0.3s（位移 10px+淡入）；全控件 transition 0.15~0.18s
- 滚动：面板/图例 8px 金色细滚动条（webkit + Firefox 双写）
- 降级：`prefers-reduced-motion` 关闭入场动画与全部 transition；`:focus-visible` 金色描边

### 5.4 响应式与触屏（已落地）

- 面板 `width:min(360px, calc(100vw-32px))`，右中定位 `top:50%` 自适应
- canvas `touch-action:none`（移动端手势前置条件）；body `overscroll-behavior:none`
- 图例 `max-height:68vh` 自滚动；搜索/按钮压边 16~20px 安全区

### 5.5 段三遗留（依赖段一，校赛前择机）

- H 键隐藏全部 UI（截图模式）、加载屏/首次引导、Top12 概念常显锚点标签（需段一新布局的层级数据）

## 六、三日排期与里程碑

| 日 | 段 | 产出 | 卡点 |
|---|---|---|---|
| 8/17 下午 | 快照 + 段一 | 新布局数据 + 四环形态截图 | 形态评审：不过则调 RING_GAP/SAT_R，不过夜 |
| 8/17 晚 | ~~段三~~ ✅ 已前置完成 | 展示层设计系统 + shot_ui 双视角截图 | — |
| 8/18 | 段二 | 点精灵+金色系截图组 | 真机 GPU 目测（headless SwiftShader 会低估加色亮度） |
| 8/19 上午 | 缓冲/真机复查 | 真机回归 + 参数微调冻结 | 不再动参数 |
| 8/19 下午 | 素材化 | BP 用图 3 张 + 15s 漫游录屏 + "按卷巡礼"一镜到底 | 交付 BP 组 |
| 8/20 | — | 报名 + BP 初稿（改造不再介入） | — |

## 七、风险与回滚

| 风险 | 概率 | 应对 |
|---|---|---|
| 日心布局后跨篇长弧穿心成乱麻 | 中 | 骨干边仅显示同环/邻环；跨环边默认隐藏、hover 才现 |
| 四环体量不均（199~363/卷）视觉失衡 | 中 | 环内角位按节点数加权分配；允许 RING_GAP 逐卷微调 |
| 点精灵拾取手感变差 | 中 | hit-proxy 不可见球兜底；threshold 0.5~2 看图调 |
| 段一形态评审不过 | 低 | 回滚快照，退守"最小方案"：旧 PCA 布局 + 概念继承卷色 + 居中校正（S2 最小化），保 8/20 节点 |
| SwiftShader 截图与真机差异 | 已知 | 段二必须真机（Edge/Chrome 独显）目测一次再定稿 |

## 八、与既有清单的对应关系

段一 = S1/S2/S3 + 数据层顺手项；段二 = H1/H2/H3/H5 + S4（团雾暂缓）+ S5；段三 = H4 + M1-M6 + S7/S10/S11 + L1-L4 精选。
暂缓项（校赛后再议）：弧线 shader 脉冲（诗云 GiftLines 式）、主题团雾团、按卷巡礼自动导览（8/19 录屏可手动飞替代）。

---

*执行原则不变：逐项修改→重建→截图→确认→下一项；所有诗云引用均已在前序报告中标注出处。*
